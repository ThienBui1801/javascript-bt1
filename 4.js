/**
 * B1: Tạo 2 biến là chiều dài và chiều rộng hình chữ nhật
 * B2: Tạo biến tính chu vi
 * B3: Tạo biến tính diện tích
 */

let long = 20;
let wide = 10;
let chuVi = (long + wide) * 2;
let dienTich = long * wide;
console.log(`Chu vi hình chữ nhật: ${chuVi} và diện tích: ${dienTich}`);